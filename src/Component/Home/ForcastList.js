import React from 'react';
import moment from 'moment';

const ForcastList = ({weather_forecast}) => {
   
        console.log('====================================')
        console.log("this is props in forecast",weather_forecast);
        console.log('====================================');
return(
  <p> ForcastList</p>

);
};
export default ForcastList;